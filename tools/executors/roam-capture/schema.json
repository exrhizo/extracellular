{
    "$schema": "http://json-schema.org/schema",
    "type": "object",
    "cli": "nx",
    "properties": {
      "port": {
        "type": "integer",
        "description": "Port to watch"
      },
      "output": {
        "type": "string",
        "description": "Destination file to write to"
      },
      "outputRaw": {
        "type": "string",
        "description": "Destination file to write raw output to"
      }
    }
  }