{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/exrhizo",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/exrhizo",
        "outputPath": "dist/apps/exrhizo"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "exrhizo:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "exrhizo:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "exrhizo:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "exrhizo:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/exrhizo"],
      "options": {
        "jestConfig": "apps/exrhizo/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/exrhizo/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
